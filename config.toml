languageCode = "en-us"
title = "Factopolis"
baseurl = "/"
GoogleAnalytics = "UA-101930305-1"
publishDir = "web"
paginate = 25
theme = "factopolis"

[taxonomies]
    claim = "claims"
    theme = "themes"
    group = "groups"

[outputs]
    page = [ "HTML", "JSON" ]
    section = [ "HTML", "JSON" ]
    home = [ "HTML", "JSON", "RSS" ]
    taxonomy = [ "HTML", "JSON" ]
    taxonomyTerm = [ "HTML", "JSON" ]

[blackfriday]
    fractions = false

[params]
# Whether to enable Dart front-end (unfinished). This will become the
# default once the Dart front-end reaches parity.
Dart = false
