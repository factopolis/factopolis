{
  "count": {{ .Data.Pages | len }},
  "people": [
	  {{ $people := .Data.Pages }}
    {{ range $idx, $person := .Data.Pages }}
      {{ if $idx }}, {{ end }}
      {
        {{ partial "person_summary.json" $person }}
      }
    {{ end }}
  ]
}